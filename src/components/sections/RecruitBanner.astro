<section id="recruit" class="section section--dark recruit" aria-labelledby="recruit-heading">
  <div class="container recruit__inner reveal">
    <p class="recruit__eyebrow label-latin">RECRUIT</p>
    <h2 id="recruit-heading" class="recruit__title">現場で、次のキャリアを。</h2>
    <p class="recruit__lead">
      ホテル・サービス業界で働きたい方、日本で活躍したい外国人材の方、セカンドキャリアとして新しい仕事に挑戦したい方——私たちと一緒に、人が育つ現場をつくりませんか。
    </p>
    <a class="btn btn--primary" href="#contact">採用についてのお問い合わせ</a>
  </div>
</section>

<style>
  .recruit__inner {
    max-width: 48rem;
    margin-inline: auto;
    text-align: center;
  }

  .recruit__eyebrow {
    margin: 0 0 var(--space-4);
    color: var(--color-accent);
  }

  .recruit__eyebrow::before {
    content: "";
    display: block;
    inline-size: 2.5rem;
    block-size: var(--rule-hair);
    margin: 0 auto var(--space-5);
    background: var(--color-rule);
  }

  .recruit__title {
    margin: 0 0 var(--space-6);
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: 500;
    letter-spacing: var(--tracking-jp);
  }

  .recruit__lead {
    margin: 0 0 var(--space-8);
    color: var(--color-text-muted);
  }
</style>
